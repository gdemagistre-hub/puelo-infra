workflows:
  ios-testflight:
    name: iOS TestFlight
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic PROX
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      vars:
        BUNDLE_ID: app.puelo
    scripts:
      - name: Prebuild iOS module
        script: |
          #!/usr/bin/env bash
          set -euo pipefail
          bash tools/codemagic_prebuild_ios.sh
      - name: Init keychain
        script: |
          keychain initialize
      - name: Fetch or create App Store signing files
        script: |
          #!/usr/bin/env bash
          set -euo pipefail
          KEY=/tmp/ios_dist_key.pem
          if [ -n "${CERTIFICATE_PRIVATE_KEY:-}" ]; then
            printf '%s\n' "$CERTIFICATE_PRIVATE_KEY" > "$KEY"
          else
            openssl genrsa -out "$KEY" 2048
            echo "Generated a new CERTIFICATE_PRIVATE_KEY. Save it in Codemagic env after this build."
          fi
          chmod 600 "$KEY"
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create \
            --certificate-key=@file:"$KEY"
      - name: Add certificates to keychain
        script: |
          keychain add-certificates
      - name: Apply signing to Xcode project
        script: |
          xcode-project use-profiles
      - name: Flutter pub get
        script: |
          flutter pub get
      - name: Build IPA
        script: |
          #!/usr/bin/env bash
          set -euo pipefail
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist \
            --dart-define=FIREBASE_API_KEY_IOS="${FIREBASE_API_KEY_IOS}" \
            --dart-define=FIREBASE_APP_ID_IOS="${FIREBASE_APP_ID_IOS}"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false
        submit_to_app_store: false
