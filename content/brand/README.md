# PROX brand sounds

Subí acá estos dos archivos (nombres exactos):

- `prox_open.mp3` — firma de ingreso (splash), ~2.4s
- `prox_confirm.mp3` — confirmación (calificación / recibo)

El CI los copia a Hosting en `content/brand/` y la app los reproduce same-origin.
